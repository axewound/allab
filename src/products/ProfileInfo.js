import React from 'react';

const ProfileInfo = (props) => {

    return (
        <div>
            <div>
111
            </div>
            <div >
                {console.log(props)}
            {/*  {props.products.title}*/}
                    {props.profile.title}
            </div>
        </div>
    )
}

export default ProfileInfo;